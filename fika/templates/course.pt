<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:i18n="http://xml.zope.org/namespaces/i18n"
      metal:use-macro="view.main_macro"
      i18n:domain="fika">
<body>

<div metal:fill-slot="content">
	<a tal:condition="view.userid and context.uid not in users.get(view.userid, None).get_field_value('courses', ())"
       href="${request.resource_url(context, 'join')}"
       class="btn btn-default glyphicon glyphicon-bookmark"
       i18n:translate="">Join</a>
    <div tal:condition="view.userid and context.uid in users.get(view.userid, None).get_field_value('courses', ())">You are doing this course. <a href="${request.resource_url(context, 'leave')}">Leave course</a></div>
	
    <tal:iterate repeat="obj course_modules">
        <div class="panel panel-default">
            <div class="panel-heading"><a href="${request.resource_url(obj)}">${obj.title}</a></div>
            <div class="panel-body">
            	<ul>
            	<tal:iterate repeat="segment sorted(obj.values(), key=lambda segment: int(segment.get_field_value('order', ())))">
            		<li>
            			${segment.get_field_value('order', ())} <a href="${request.resource_url(segment)}">${segment.title}</a> (${segment.__class__.__name__})
            			<tal:form  replace="structure segment.render(request, segment)"></tal:form>
            		</li>
            	</tal:iterate>
            	</ul>
            </div>
        </div>
    </tal:iterate>
</div>

</body>
</html>
